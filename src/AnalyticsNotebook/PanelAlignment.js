/**
 * @typedef {String} PanelAlignment
 */

/**
 * @enum {PanelAlignment}
 */
var PANEL_ALIGNMENT = {
  /**
   * Child panels will be aligned horizontally within the parent.
   */
  HORIZONTAL: "horizontal",
  /**
   * Child panels will be aligned vertically within the parent.
   */
  VERTICAL: "vertical",
};

export default PANEL_ALIGNMENT;
